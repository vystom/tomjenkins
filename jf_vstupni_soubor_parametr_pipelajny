pipeline {
  agent any
  
  stages {
    stage('Example') {
      steps {
        //echo "clent.txt="+${client.txt}"      
          // sh 'cat $THEFILE'

        sh "echo $FILE | base64 -d"

      }


    }
  }
}