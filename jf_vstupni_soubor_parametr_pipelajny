pipeline {
  agent any
  stages {
    stage('Example') {
      steps {
        script{
          sh "ls -la"
          sh "echo pracprostor = ${WORKSPACE}"
          sh "echo jenkins home = ${JENKINS_HOME}"
          sh "echo "${JENKINS_HOME} - jobs - ${JOB_NAME} - builds - ${BUILD_ID} - fileParameters\\myUploadedFile""
          
        }
      }
    }
  }
}