parameters{
        file name:'client.txt', description:'vloz client.txt'
   }



pipeline {
    agent any
    stages {
        stage('Nacteni souboru') {
            steps {
                script {
                    def myfile = readFile('client.txt')
                    echo "${myfile}"
                }
            }
        }
    }
}
